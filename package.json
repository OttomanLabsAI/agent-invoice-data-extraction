{
  "name": "agent-invoice-data-extraction",
  "version": "2.2.0",
  "private": true,
  "type": "module",
  "description": "Glent Group invoice intake agent: a Cloudflare Worker that reads supplier invoices from Gmail with Claude and prepares Sage Intacct bills",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "wrangler deploy --dry-run",
    "test": "node --test tests/unit.test.js",
    "smoke": "node tests/smoke.js"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
