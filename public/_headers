/*
  X-Content-Type-Options: nosniff
  X-Frame-Options: DENY
  Referrer-Policy: strict-origin-when-cross-origin
  Permissions-Policy: geolocation=(), microphone=(), camera=(), interest-cohort=()
  Strict-Transport-Security: max-age=31536000; includeSubDomains

# Static assets are safe to cache hard - bump the ?v= query in the HTML (or rename the file) to bust.
/assets/*
  Cache-Control: public, max-age=31536000, immutable

# HTML must stay fresh so a deploy is visible immediately.
/*.html
  Cache-Control: public, max-age=0, must-revalidate

/
  Cache-Control: public, max-age=0, must-revalidate

# Downloads must revalidate so a rebuilt updater zip is picked up straight away.
/downloads/*
  Cache-Control: public, max-age=0, must-revalidate
